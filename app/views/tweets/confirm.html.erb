<h3>以下の内容で、ツイートする。</h3>

<%= form_with(model: @tweet, url: tweets_path, local: true) do |form| %>
  <%= form.hidden_field :content %>

  <p>ツイート内容:<%= @blog.content %></p>
  <%= form.submit  "ツイートする" %>
<% end %>
