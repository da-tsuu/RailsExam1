<%= form_with(model: Tweet.new, local: true) do |form| %>
  <div class="tweet_content">
    <%= form.label :Tweet! %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>
